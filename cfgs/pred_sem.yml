# Dataset in use
data:
  name: vrd

# Architecture (or model) type
model:
  use_pred_sem: True
  n_fus_neurons: 256

# Evaluation Arguments
eval:
  use_preload: False
  eval_obj: False
  test_rel: False

# Training parameters
training:
  num_epochs: 4
  checkpoint_freq: 0
  loss: mlab
  # loss: mse
  use_preload: False
  test_freq: 2
