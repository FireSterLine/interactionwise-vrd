# Dataset in use
data:
  name: vrd

# Architecture (or model) type
#model:
    #  use_pred_sem: True

# Evaluation Arguments
eval:
  eval_obj: False
  test_rel: False
  rec: [50, 30]

# Training parameters
training:
  num_epochs: 5
  checkpoint_freq: 0
  loss: mlab
  test_freq: [2,3,4]
